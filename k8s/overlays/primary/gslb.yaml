apiVersion: k8gb.io/v1beta1
kind: Gslb
metadata:
  name: marketpay
  namespace: stellar-marketpay
spec:
  resourceRef:
    apiVersion: networking.k8s.io/v1
    kind: Ingress
    name: marketpay
  strategy:
    type: failover
    primaryGeoTag: primary-cluster
    dnsTtlSeconds: 30
